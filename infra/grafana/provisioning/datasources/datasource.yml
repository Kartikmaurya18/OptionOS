apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # Container-to-container: Grafana and Prometheus are on the same
    # compose network, so they reach each other by service name.
    url: http://prometheus:9090
    isDefault: true
    editable: false
